# shadcn/ui 样式对齐参考

> 本文档记录了从 shadcn/ui 官网提取的真实样式数据，并说明了如何将这些样式应用到项目中。

## 📊 提取的样式数据

### 布局尺寸

| 元素 | shadcn/ui 实际值 | 我们的调整 | Tailwind 类 |
|------|------------------|------------|-------------|
| **Sidebar 宽度** | 224px | 224px | `w-56` |
| **Header 高度** | 56px | 56px | `h-14` |
| **主内容最大宽度** | 1200px | 1200px | `max-w-screen-xl` |

### 导航链接样式

| 属性 | shadcn/ui 实际值 | 我们的调整 |
|------|------------------|------------|
| **字体大小** | 12.8px | 13px (`text-[13px]`) |
| **字体粗细** | 500 | 500 (`font-medium`) |
| **Padding** | 8px | 8px (`p-2`) |
| **Border Radius** | 8px | 8px (`rounded-lg`) |
| **普通状态颜色** | `lab(2.75381 0 0)` (深灰) | `text-foreground/80` |
| **普通状态背景** | 透明 | 透明 |
| **激活状态颜色** | `lab(7.78201 -0.0000149012 0)` (黑色) | `text-foreground` |
| **激活状态背景** | `lab(96.52 -0.0000298023 0.0000119209)` (浅灰) | `bg-muted` |
| **Hover 背景** | `bg-muted/50` | `bg-muted/50` |

### 标题样式

| 元素 | shadcn/ui 实际值 | 我们的调整 |
|------|------------------|------------|
| **H1 字体大小** | 30px | 30px (`1.875rem`) |
| **H1 字体粗细** | 600 | 600 (`font-semibold`) |
| **H1 行高** | 36px | 1.2 |
| **H1 字间距** | -0.75px | `-0.75px` |

### 分组标题样式

| 属性 | shadcn/ui 实际值 | 我们的调整 |
|------|------------------|------------|
| **字体大小** | 12px | 12px (`text-xs`) |
| **字体粗细** | 500 | 500 (`font-medium`) |
| **颜色** | `lab(48.496 0 0)` (灰色) | `text-muted-foreground` |
| **文本转换** | none | 移除了 `uppercase` |

## 🎨 关键改进

### 1. 导航链接设计优化

**之前的设计问题：**
- ❌ 使用左侧蓝色竖线作为激活指示器
- ❌ 激活状态使用 `bg-primary/10` (太浅)
- ❌ 字体大小为 14px (偏大)
- ❌ Padding 不统一 (`px-3 py-2`)

**新设计：**
- ✅ 使用浅灰色背景 `bg-muted` 表示激活状态（更符合 shadcn/ui）
- ✅ 字体大小为 13px（更紧凑）
- ✅ 统一 padding: 8px (`p-2`)
- ✅ 圆角为 8px (`rounded-lg`)
- ✅ Hover 状态使用 `bg-muted/50`

### 2. 侧边栏宽度调整

```tsx
// 之前: w-64 (256px)
sidebarCollapsed ? 'w-16' : 'w-64'

// 现在: w-56 (224px)
sidebarCollapsed ? 'w-16' : 'w-56'
```

### 3. Header 高度调整

```tsx
// 之前: h-16 (64px)
<header className="h-16 ...">

// 现在: h-14 (56px) - 与 shadcn/ui 一致
<header className="h-14 ...">
```

### 4. 标题层次优化

```css
/* 之前 */
h1 {
  font-size: 2.25rem; /* 36px */
  font-weight: bold;
  tracking-tight;
}

/* 现在 */
h1 {
  font-size: 1.875rem; /* 30px */
  font-weight: 600;
  letter-spacing: -0.75px;
}
```

## 📸 参考截图

参考截图已保存至：
- `docs/ui/reference/shadcn-button-page.png`

## 🛠️ 实施文件

以下文件已更新以匹配 shadcn/ui 样式：

1. **Sidebar.tsx**
   - 宽度: `w-64` → `w-56`
   - 背景: `bg-bg-secondary` → `bg-card`
   - 链接字体: `text-sm` → `text-[13px]`
   - 链接 padding: `px-3 py-2` → `p-2`
   - 激活状态: `bg-primary/10 + 左侧竖线` → `bg-muted`
   - 圆角: `rounded-md` → `rounded-lg`

2. **Header.tsx**
   - 高度: `h-16` → `h-14`

3. **index.css**
   - H1 字体大小: `2.25rem` → `1.875rem`
   - H1 字体粗细: `bold` → `600`
   - H1 字间距: 添加 `-0.75px`
   - `.text-h1` 工具类同步更新

## 🎯 视觉对比

### 导航链接激活状态

| 之前 | 现在 (shadcn/ui 风格) |
|------|----------------------|
| 蓝色背景 + 左侧蓝色竖线 | 浅灰色背景 |
| `bg-primary/10` + border | `bg-muted` |
| 较大的 padding | 紧凑的 8px padding |

### 整体布局

| 之前 | 现在 (shadcn/ui 对齐) |
|------|----------------------|
| Sidebar: 256px | Sidebar: 224px |
| Header: 64px | Header: 56px |
| H1: 36px bold | H1: 30px semibold |

## 📝 注意事项

1. **颜色映射**：shadcn/ui 使用 LAB 颜色空间，我们使用 Tailwind 的语义化颜色变量（`muted`, `foreground` 等）来近似匹配。

2. **响应式设计**：这些调整主要针对桌面端，移动端的折叠侧边栏宽度保持不变 (`w-16`)。

3. **字体**：shadcn/ui 使用 Geist 字体，我们使用 `font-sans`（默认系统字体栈）。

4. **动画**：保持了过渡动画 `transition-colors duration-150`。

## ✅ 完成状态

所有 shadcn/ui 样式对齐任务已完成：

- [x] 调整导航链接字体大小 14px → 13px
- [x] 调整 H1 标题为 30px + font-weight 600 + letter-spacing -0.75px
- [x] 调整侧边栏宽度为 224px
- [x] 调整 Header 高度为 56px
- [x] 优化导航链接激活状态 - 使用浅色背景而非左侧竖线
- [x] 调整导航链接 padding 为 8px + border-radius 8px

## 🔗 相关资源

- [shadcn/ui 官网](https://ui.shadcn.com/)
- [shadcn/ui Components](https://ui.shadcn.com/docs/components)
- [项目 UI 文档](../README.md)
